<?xml version="1.0" encoding="UTF-8"?><sqlb_project><db path="C:/Users/alexw/StudioProjects/device-explorer/SM-T970/mymonma/monma.db" readonly="0" foreign_keys="1" case_sensitive_like="0" temp_store="0" wal_autocheckpoint="1000" synchronous="2"/><attached/><window><main_tabs open="structure browser pragmas query" current="3"/></window><tab_structure><column_width id="0" width="300"/><column_width id="1" width="0"/><column_width id="2" width="100"/><column_width id="3" width="7163"/><column_width id="4" width="0"/><expanded_item id="0" parent="1"/><expanded_item id="1" parent="1"/><expanded_item id="2" parent="1"/><expanded_item id="3" parent="1"/></tab_structure><tab_browse><current_table name="4,7:mainAccount"/><default_encoding codec=""/><browse_table_settings/></tab_browse><tab_sql><sql name="SQL 1">update cat set saldo = (
select sum(amount) 
from CashTrx a 
where (transferid is null or isUmbuchung )
and accountid = 7)
where id = 7</sql><sql name="SQL 2">select name, sum(amount) 
- (select sum(amount) from CashTrx b where b.catid = a.accountid) 
from CashTrx a 
join cat b on a.accountid = b.id 
where transferid is null 
group by accountid</sql><sql name="SQL 3">select * from CashTrx where accountid = 16 -- or catid = 16 
order by btag desc </sql><sql name="SQL 4">insert into CashTrx (btag, partnerid,accountid,  catid, amount, memo, transferid)
select btag, partnerid,catid, accountid, -amount, memo, id 
from CashTrx where catid between 1 and 99 and transferid is null</sql><sql name="SQL 6">--explain query plan
        select a.*
        ,(select name from Partnerstamm  p where p.id = a.partnerid) as partnername
        ,(select name from Cat c where c.id = a.accountid) as accountname
		,c.name as catname, c.catclassid
--		,t.id as importTrxId
        from CashTrx a 
        left join Cat c on c.id = catid
--        left outer join ImportTrx t on a.id = t.umsatzid 
		where a. accountid = 46
		        and (transferid is null or isUmbuchung)
        order by btag desc, a.id

</sql><sql name="SQL 8">select accountid, total(marktwert)   
                from (  
                select accountid,  sum(menge) / 100000 *   
                (SELECT kurs from WPKurs d where a.wpid = d.wpid 
                group by wpid having max(btag)) as marktwert   
                from WPTrx a   
                where paketid is null   		
                group by accountid, wpid)  
group by accountid
</sql><sql name="SQL 30">
select accountid, wpid,
 sum(menge) / 100000 * (SELECT kurs from WPKurs d where a.wpid = d.wpid group by wpid having max(btag)) as marktwert   
 from WPTrx a   
 where paketid is null   		
 group by accountid, wpid</sql><sql name="SQL 9">--explain query plan
select a.*, sum(menge) as bestand
--,(select sum(menge) from WPTrx b  where a.wpid = b.wpid and paketid is null  and b.btag &lt;= a.btag) 

from WPKurs a
left outer join WPTrx b on a.wpid = b.wpid and paketid is null  and b.btag &lt;= a.btag
group by a.wpid, a.btag having bestand &gt; 0
order by a.btag desc
</sql><sql name="SQL 10">        select a.*, b.name as obercatname
        ,(select sum(amount) from CashTrx where accountid = a.id and (transferid is null or isUmbuchung)) as saldo
        from cat a
        join Cat b on(a.obercatid = b.id)
        join Account b using(id)
        where a.supercatid = 1001
        and a.catclassid != 1
</sql><sql name="SQL 11">        select sum(amount) 
		,(	   select total(marktwert)
			from (
        select accountid,  sum(menge) / 100000 *
        (SELECT kurs from WPKurs d where a.wpid = d.wpid
        group by wpid having max(btag)) as marktwert
        from WPTrx a
        where paketid is null 
        group by accountid, wpid)
)
        from CashTrx a 
        where (transferid is null or isUmbuchung )
</sql><sql name="SQL 12">        select total(marktwert)
        from (
        select accountid,  sum(menge) / 100000 *
        (SELECT kurs from WPKurs d where a.wpid = d.wpid
        group by wpid having max(btag)) as marktwert
        from WPTrx a
        where paketid is null
        group by accountid, wpid)
</sql><sql name="SQL 32">        select DISTINCT(verrechnungskonto), * from AccountDepotView</sql><current_tab id="11"/></tab_sql></sqlb_project>
